netmiko>=4.3,<5
# paramiko 4.x removed ssh-dss host key support, which old ERS/BOSS
# switches still present - stay on 3.x for legacy SSH compatibility
paramiko>=3.4,<4
PyYAML>=6.0
rich>=13.0
openpyxl>=3.1
