# Inventory of the switches to be migrated.
# Copy to switches.yaml (gitignored) and fill in.
#
#   name:     display name used in the report
#   host:     IP or DNS name for SSH (optional - defaults to `name`)
#   platform: voss | ers
#   console:  console-server line for a switch WITHOUT a management IP
#             (optional; needs the console_server section in config.yaml)
#
switches:
  - name: old-access-01
    host: 10.1.1.10
    platform: ers
  - name: old-access-02
    host: 10.1.1.11
    platform: ers
  - name: old-agg-01
    host: 10.1.1.20
    platform: voss
  # - name: new-leaf-01        # no mgmt IP yet: reached via the console server
  #   platform: voss
  #   console: "03"
